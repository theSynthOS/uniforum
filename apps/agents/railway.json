{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "pnpm install --frozen-lockfile && pnpm build --filter @uniforum/agents-service",
    "watchPatterns": ["apps/agents/**", "packages/contracts/**", "packages/forum/**", "packages/shared/**"]
  },
  "deploy": {
    "startCommand": "pnpm --filter @uniforum/agents-service start"
  }
}
