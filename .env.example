# ============================================
# UNIFORUM ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env.local and fill in your values

# --------------------------------------------
# AI / LLM
# --------------------------------------------
OPENAI_API_KEY=sk-...
# Optional: Alternative LLM provider
# ANTHROPIC_API_KEY=sk-ant-...

# --------------------------------------------
# PRIVY (Authentication & Wallets)
# --------------------------------------------
# Get these from https://dashboard.privy.io
# App ID - same across all environments
NEXT_PUBLIC_PRIVY_APP_ID=
# App Client ID - use different client IDs for dev/staging/prod
# Create clients at: Dashboard > Configuration > App settings > Clients
NEXT_PUBLIC_PRIVY_APP_CLIENT_ID=
# App Secret - for server-side token verification
PRIVY_APP_SECRET=

# --------------------------------------------
# BLOCKCHAIN / RPC
# --------------------------------------------
# Alchemy or Infura API key for RPC access
ALCHEMY_API_KEY=
# Or direct RPC URLs
UNICHAIN_SEPOLIA_RPC_URL=https://sepolia.unichain.org
UNICHAIN_MAINNET_RPC_URL=https://mainnet.unichain.org

# --------------------------------------------
# FOR SIMULATING EXECUTION (swap / addLiq / removeLiq / limitOrder)
# --------------------------------------------
# Minimum to run simulations (test:execution-all-actions, test:execution-tx):
#   UNICHAIN_SEPOLIA_RPC_URL   ← required for simulation + Quoter (use public RPC or your own)
#
# Optional – to actually send a tx (not just simulate):
#   TEST_EXECUTOR_PRIVATE_KEY  ← executor wallet key (hex); only set if you want to broadcast
#
# Optional – for API execution-payload enrichment (real tokens, pool, amountOutMinimum):
#   TOKEN_LIST_URL             ← JSON URL, Uniswap format: { "tokens": [{ "address", "symbol", "chainId" }] }
#   TOKEN_LIST_URL_1301        ← or per-chain (1301 = Unichain Sepolia)
#   GRAPH_API_KEY              ← The Graph gateway key (https://thegraph.com/studio) for v4 subgraph
#   UNISWAP_V4_SUBGRAPH_URL    ← or full subgraph URL instead of GRAPH_API_KEY
# (Quoter uses UNICHAIN_SEPOLIA_RPC_URL; no separate key needed)

# --------------------------------------------
# EXECUTION PAYLOAD ENRICHMENT (optional)
# --------------------------------------------
# Same as above: TOKEN_LIST_*, GRAPH_API_KEY / UNISWAP_V4_SUBGRAPH_URL, RPC for Quoter

# --------------------------------------------
# SUPABASE (Server-side only - NOT exposed to client)
# --------------------------------------------
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...
# Project ID for type generation
SUPABASE_PROJECT_ID=your-project-id

# --------------------------------------------
# ENCRYPTION
# --------------------------------------------
# Used to encrypt agent private keys at rest
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=

# --------------------------------------------
# APP CONFIG
# --------------------------------------------
NEXT_PUBLIC_APP_URL=https://uniforum.synthos.fun
NEXT_PUBLIC_API_URL=https://api-uniforum.synthos.fun

# --------------------------------------------
# SERVER PORTS (for local development)
# --------------------------------------------
# API server port (Hono backend)
PORT=3001

# --------------------------------------------
# DEVELOPMENT
# --------------------------------------------
# Set to 'development' or 'production'
NODE_ENV=development
