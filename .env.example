# ============================================
# UNIFORUM ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env.local and fill in your values

# --------------------------------------------
# AI / LLM
# --------------------------------------------
OPENAI_API_KEY=sk-...
# Optional: Alternative LLM provider
# ANTHROPIC_API_KEY=sk-ant-...

# --------------------------------------------
# PRIVY (Authentication & Wallets)
# --------------------------------------------
# Get these from https://dashboard.privy.io
# App ID - same across all environments
NEXT_PUBLIC_PRIVY_APP_ID=
# App Client ID - use different client IDs for dev/staging/prod
# Create clients at: Dashboard > Configuration > App settings > Clients
NEXT_PUBLIC_PRIVY_APP_CLIENT_ID=
# App Secret - for server-side token verification
PRIVY_APP_SECRET=

# --------------------------------------------
# BLOCKCHAIN / RPC
# --------------------------------------------
# Alchemy or Infura API key for RPC access
ALCHEMY_API_KEY=
# Or direct RPC URLs
UNICHAIN_SEPOLIA_RPC_URL=https://sepolia.unichain.org
UNICHAIN_MAINNET_RPC_URL=https://mainnet.unichain.org

# --------------------------------------------
# FOR SIMULATING EXECUTION (swap / addLiq / removeLiq / limitOrder)
# --------------------------------------------
# Minimum: UNICHAIN_SEPOLIA_RPC_URL (above). Optional keys below.
TEST_EXECUTOR_PRIVATE_KEY=
# Token list (optional; default Uniswap list used if unset)
TOKEN_LIST_URL=
TOKEN_LIST_URL_1301=
TOKEN_LIST_URL_130=
# Subgraph: get free key at https://thegraph.com/studio/apikeys/
GRAPH_API_KEY=
UNISWAP_V4_SUBGRAPH_URL=
# RPC_URL_1301= / RPC_URL_130= optional overrides per chain

# --------------------------------------------
# SUPABASE (Server-side only - NOT exposed to client)
# --------------------------------------------
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...
# Project ID for type generation
SUPABASE_PROJECT_ID=your-project-id

# --------------------------------------------
# ENCRYPTION
# --------------------------------------------
# Used to encrypt agent private keys at rest
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=

# --------------------------------------------
# APP CONFIG
# --------------------------------------------
NEXT_PUBLIC_APP_URL=https://e3ca-2001-f40-9a4-9909-c174-3ed7-2e4a-bb9e.ngrok-free.app
NEXT_PUBLIC_API_URL=https://api-uniforum.up.railway.app
# Internal API base URL used by agents service (override in dev)
UNIFORUM_API_URL=http://localhost:3001
# Chain ID used by the execution worker (default 1301)
EXECUTION_CHAIN_ID=1301

# --------------------------------------------
# ENS CCIP-READ (Offchain Resolver)
# --------------------------------------------
# Deployed OffchainResolver contract address on Ethereum mainnet
ENS_OFFCHAIN_RESOLVER_ADDRESS=
# Gateway signer private key (used to sign CCIP-Read responses)
ENS_CCIP_SIGNER_PRIVATE_KEY=
# Optional: Override gateway URL used by the resolver (if different from API URL)
ENS_CCIP_GATEWAY_URL=https://api-uniforum.up.railway.app/v1/ens

# --------------------------------------------
# ENS CCIP-READ DEPLOYMENT (Mainnet)
# --------------------------------------------
# Deployer key for OffchainResolver deployment
DEPLOYER_PRIVATE_KEY=
# Address that will be authorized to sign CCIP-Read responses
ENS_CCIP_SIGNER_ADDRESS=
# Optional: Mainnet RPC URL override
ETH_RPC_URL=
# Optional: Sepolia RPC URL (for OffchainResolver deployment)
SEPOLIA_RPC_URL=

# --------------------------------------------
# SERVER PORTS (for local development)
# --------------------------------------------
# API server port (Hono backend)
PORT=3001

# --------------------------------------------
# DEVELOPMENT
# --------------------------------------------
# Set to 'development' or 'production'
NODE_ENV=development
